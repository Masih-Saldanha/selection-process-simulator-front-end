import { FilterApprovedListByNamePipe } from './filter-approved-list-by-name.pipe';

describe('FilterApprovedListByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterApprovedListByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
